# Copy this file to .env.local and paste your key.
# Get one at https://console.groq.com/keys
#
# This is read server-side only. Never rename it to NEXT_PUBLIC_GROQ_API_KEY —
# that prefix ships the value to the browser for anyone to read.
GROQ_API_KEY=
